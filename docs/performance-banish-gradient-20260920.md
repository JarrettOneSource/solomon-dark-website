The banish death-effect renderer now retains at most six owned gradient canvases per effect. It previously created and destroyed six Pixi `FillGradient` textures on every visible update. Pixi's installed `FillGradient.buildLinearGradient()` creates a fresh 256 × 1 DOM canvas, so that path generated repeated DOM canvas, texture, and graphics-resource allocation during banish animations.

The first Windows allocation profile contains the concrete `updateBanish → gradientRect → buildLinearGradient` call path. Its sampled inclusive allocation estimates were 6.316 MiB under `updateBanish`, 4.753 MiB under `gradientRect`, and 2.001 MiB under `buildLinearGradient` during 31.196052 seconds. These nested totals overlap. The CPU profile attributed 133.509 ms inclusively to `updateBanish`. The profiles include collected allocations; they do not establish a retained-memory leak or explain every long-session collection pause.

The source change is limited to `frontend/src/game/renderer/native-enemy-death-effect-view.ts`. Native progress, geometry, sprite placement, alpha, tint, painter order, and blend mode formulas are unchanged. The view reuses its gradient objects, applies Pixi's existing `Color.shared.setValue(...).toHexa()` normalization, and repaints a gradient only when its normalized stops change. It clears the owned canvas before painting, preserving the transparent pixels of a newly created gradient, then sends the standard texture-source update notification. Effect retirement still destroys every owned gradient source. Shared art textures remain owned by the texture manager.

An isolated Chrome 153.0.8010.48 comparison used the original source and the changed source with actual Pixi objects and real Canvas2D pixels. It covered ordinary and black banishes, scales 0.75, 1, and 2, view heights 720 and 1080, 257 lifetime positions plus fractional, repeated, expired, and restarted states. Each mode compared 3,144 complete view samples across 12 effects. It checked exact gradient RGBA bytes, normalized stops, texture transforms and dimensions, graphics paths and styles, container placement, and sprite properties with no numeric tolerance.

| Comparison mode | Original canvases | Retained canvases | Exact output mismatches |
| --- | ---: | ---: | ---: |
| Both source canvases created with `willReadFrequently` | 18,720 | 72 | 0 |
| Default source contexts, copied to a separate readback canvas | 18,720 | 72 | 0 |

Canvas creation fell 99.615% in each comparison, from 1,560 to 6 per effect. All 72 retained texture sources were destroyed after their effects in each mode. The comparison used headless Chrome solely for correctness and object creation counts; it is not a live frame-time benchmark. Its two modes ran from 2026-09-20 18:44:05.086 UTC to 18:44:26.988 UTC under Node 22.17.0.

The initial comparison directly called `getImageData` repeatedly on retained default-context source canvases while reading each newly created baseline canvas once. It found a one-unit red-channel difference at ordinary banish scale 0.75, height 720, age 0.29296875. No runtime change or tolerance relaxation resolved that comparison: the two symmetric methods above both passed exactly. The difference was associated with asymmetric readback instrumentation; a browser rasterization-backend switch was not directly traced.

The seven-test renderer ownership suite passed under Node 22.17.0. The new regression in `native-mesh-lifetime.test.ts` checks lazy offscreen allocation, six-canvas reuse, native color normalization, clear-before-repaint behavior, texture update notifications, unchanged-color reuse, collapsed and restored geometry, and immediate unload/destruction on effect retirement. That existing test file is now explicitly included in `tsconfig.test.json`; `test:boneyard` already runs it. The first focused invocation exposed a test-loader JSON import issue, corrected by loading this renderer through the existing Vite SSR test loader. Expanded typechecking also exposed previously unchecked fixture types: the weather callback now uses the native method signature, the renderer fixture constructs a concrete uninitialized `WebGLRenderer`, and the golem fixture supplies its required `circleSlowTicks`. The expanded canonical typecheck passed after those repairs, and the coordinated four-suite renderer run passed 39/39 tests, including all seven ownership tests. Their logs and command receipt are in `/tmp/solomon-spell-geometry-6a583nbx`. The parent task coordinates the final lint and repository gate after all fixes finish.

Reproduction code, the original source snapshot, and detailed receipts are under `/tmp/solomon-banish-gradient-6a583nbx`. The original renderer SHA-256 is `20301c6c0e107a414cafd40449b73732951a1aeb5914b2f0274f681bc829fe5c`; the changed renderer is `027c7cf312f2255f816f1a1f21979d384f3ac6168a011ac833fdca512af3c845`. The structured report is `docs/performance-banish-gradient-20260920.json`.

The completed two-client attempt that stopped at wave 28 preceded this change. Its DOM counters do not identify node types or retaining paths, so the collected 14,087-node drop has not been wholly attributed to banish canvases. A new endurance run is still needed to assess the remaining frame-time hitches with this fix included. No live browser profiler, forced collection, native gameplay rule change, commit, push, or deployment was used for this contribution.
