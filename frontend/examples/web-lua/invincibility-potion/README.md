# Invincibility Potion

This is the canonical first-party Web Lua 1.0 package. It preserves package ID
`canary.lua.invincibility_potion` and content ID `8068156596081641415` while
using the Website-only definition graph, typed art, automatic timed status,
loot, save, replication, and presentation owners.

Validate or pack it from `frontend/`:

```sh
npm run sdmod -- check examples/web-lua/invincibility-potion
npm run sdmod -- pack examples/web-lua/invincibility-potion invincibility-potion.sdmod
```
